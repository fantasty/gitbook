# 二分查找

```cpp
int BinarySearch(int *number, int len, int value)
{
    if (number == NULL || len <= 0)
        return -1;
    int begin = 0;
    int end = len - 1;
    while (begin <= end)
    {    
        int mid = (begin + end) / 2;
        if (number[mid] == value)
            reutrn mid;
        if (number[begin] < number[mid])
            begin = mid + 1;
        else if (number[mid] < number[end])
            end = mid - 1;
    }
    return -1;
}
```



